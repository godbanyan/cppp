/**
 * Exercise 11.14: Extend the map of children to their family name that you wrote for the
 * exercises in c11.7 by having the vector store a pair that holds a child’s
 * name and birthday.
 */

#include <map>
#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
    map<string, vector<pair<string, string>>> families;
    
    string family;
}